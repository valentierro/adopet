# Store do pnpm dentro do projeto (evita permissão em ~/.pnpm-store)
store-dir=.pnpm-store
# Layout hoisted para monorepo: dependências no root e resolução correta no build (API, Vercel)
node-linker=hoisted
# Copiar pacotes em vez de symlink (evita ENOENT no build Vercel ao resolver .pnpm)
package-import-method=copy
# Prisma no monorepo: hoist para o build (ex.: Vercel) resolver @prisma/client
public-hoist-pattern[]=@prisma/client
public-hoist-pattern[]=.prisma
public-hoist-pattern[]=@nestjs/config
